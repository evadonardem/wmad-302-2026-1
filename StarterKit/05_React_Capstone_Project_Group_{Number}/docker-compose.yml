services:
  app:
    build: .
    container_name: "react_ph_tourist_spot_searcher"
    ports:
      - "8082:5173"
    volumes:
      - .:/app
    stdin_open: true
    tty: true
    command: sh -c "npm install && npm run dev -- --host"
